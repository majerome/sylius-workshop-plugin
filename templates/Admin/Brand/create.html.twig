{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.code) }}
    {{ form_row(form._token) }}
<button type="submit" class="btn btn-primary">{{ 'sylius.ui.create'|trans }}</button>
{{ form_end(form) }}
