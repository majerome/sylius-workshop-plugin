{% set form = hookable_metadata.context.form %}
{% set product = hookable_metadata.context.resource %}

<div class="tab-pane {% if hookable_metadata.configuration.active|default(false) %}show active{% endif %}" id="brand" role="tabpanel" tabindex="0">
    <div class="card mb-3">
        <div class="card-header">
            <h2 id="productBrand" class="card-title">
                {{ 'majerome_workshop_plugin.ui.brands'|trans }}
            </h2>
        </div>
        <div class="card-body">
            {{ form_row(form.brand) }}
        </div>
    </div>
</div>
